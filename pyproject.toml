[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "CFMS_Server"
authors = [{name = "Creeper19472", email = "creeper19472@crpteam.club"}]
dynamic = ["version", "description"]
